#! /bin/sh
output="$HOME/Pictures/scrot/%Y-%m-%d-%T-full.png"
error () {
  notify-send "Error!"
  exit
}
scrot "$output" -e "xclip -selection clipboard -target image/png -i $output" || error
notify-send "OK?"
